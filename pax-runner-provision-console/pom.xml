<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <relativePath>../</relativePath>
    <groupId>org.ops4j.pax.runner.pax-runner-build</groupId>
    <artifactId>pax-runner-build-root</artifactId>
    <version>0.5.6</version>
  </parent>

  <groupId>org.ops4j.pax.runner.pax-runner-internal</groupId>
  <artifactId>pax-runner-provision-console</artifactId>
  <version>0.5.6</version>
  <packaging>bundle</packaging>

  <name>OPS4J Pax Runner - Provision Console</name>
  <description>
    OPS4J Pax Runner - Utility console that could be used to provision from a command line while in a running osgi platform. See details at http://wiki.ops4j.org/confluence/x/A4A6
  </description>

  <url>http://wiki.ops4j.org/confluence/x/A4A6</url>

  <properties>
    <bundle.package>org.ops4j.pax.runner.provision.console</bundle.package>
  </properties>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-dependency-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>${maven.bundle.plugin.groupId}</groupId>
        <artifactId>maven-bundle-plugin</artifactId>
        <extensions>true</extensions>
        <configuration>
          <instructions>
            <Export-Package>
              !${bundle.package}.internal
            </Export-Package>
          </instructions>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <dependency>
      <groupId>org.ops4j.pax.runner</groupId>
      <artifactId>pax-runner-provision</artifactId>
      <version>0.5.6</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

</project>